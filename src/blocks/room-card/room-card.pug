include ../slider/slider.pug
include ../room-number/room-number.pug
include ../rate-button/rate-button.pug

mixin room-card(image, image1, image2, image3, num, number, price, stars=0)
	.room-card
		.room-card__slider
			+slider(image, image1, image2, image3)
		.room-card__container
			.room-card__number
				+room-number(number, price)
		.room-card__container
			.room-card__stars
				+rate-button(stars)
			.room-card__reviews
				span.room-card__reviews-num= num
				span.room-card__reviews-text Отзывов

