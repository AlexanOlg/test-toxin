mixin palette(header, signature)
    .palette
        .palette__item(class!=attributes.class)
        .palette__text
            span.palette__header= header 
            br
            span.palette__signature= signature